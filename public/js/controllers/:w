<form id="metadataForm" ng-controller="FormCreateCtrl">
    <h1>{{center}}</h1>
    <section class="row">
        <h3 class="col-sm-12">Metadata</h3>
        <lm-form-field ng-repeat="field in fields"
        title="{{field.title}}"
        max-tags="{{field.maxTags}}"
        placeholder="{{field.placeholder}}",
        ng-model="field.model"
        autocomplete-source="autocompleteSource()"
        ></lm-form-field>
    </section>
    <section class="row">
        <h3 class="col-sm-12">Release Dates</h3>
        <lm-date-picker ng-repeat="date in releaseDates"
            level="{{date.level}}"
            ng-model="date.model.val"
        ></lm-date-picker>
    </section>
    <section class="row">
        <h3 class="col-sm-12">URLs</h3>
        <label ng-repeat="url in urls" class="col-sm-6">
            {{url.title}}
            <input type="text" ng-model="url.model.val">
        </label>
    </section>
    <section>
        <button type="button" ng-click="submit()">Submit</button>
    </section>
</form>
